import React from 'react'
import Button from './Button'

const SearchButton = () => (
  <Button handleClick={() => console.log('search button')} >
    Search Button
  </Button>
)

export default SearchButton
